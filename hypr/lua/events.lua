--------------------------------------------------------------------------------
-- Hyprland — events.lua
-- Runtime event handlers (workspace changes, window events, etc.)
--------------------------------------------------------------------------------

-- (Waybar hide-on-workspace-2 removed; terminal stays fullscreen instead.)
